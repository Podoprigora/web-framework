{
  "scripts": {
    "start:db": "json-server --watch  json_server/db.json --port 0001 ",
    "start:parcel": "parcel  index.html"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}